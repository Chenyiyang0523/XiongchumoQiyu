# 美术源文件说明

`background-variants/` 保存未进入当前版本的背景候选。当前游戏只使用 `game/images/bg/` 中六张规范化 WebP：forest、cabin、cave、riverside、mountain、village。

`rejected-ui/` 保存带有棋盘格或不适合直接发布的旧 UI 位图，仅供对照。运行时选择框已改为代码生成的纯色框，不再依赖这些文件。

在确认来源、授权、作者署名和可修改范围前，不应把本目录的候选资产重新放入发行包。
